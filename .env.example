# =============================================================================
# HubSpot AI Wrapper - Environment Variables
# Copy to .env (root), .env.local (frontend), or set in deployment platforms
# =============================================================================

# -----------------------------------------------------------------------------
# Backend (FastAPI) - Railway / Render
# -----------------------------------------------------------------------------
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# HubSpot
HUBSPOT_ACCESS_TOKEN=your-hubspot-private-app-token
HUBSPOT_CLIENT_ID=your-hubspot-client-id
HUBSPOT_CLIENT_SECRET=your-hubspot-client-secret

# LLM (e.g. OpenAI for AI processing)
OPENAI_API_KEY=your-openai-api-key

# Backend URL (for CORS; set in production)
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Frontend (Next.js) - Vercel
# -----------------------------------------------------------------------------
# Point to backend API
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase (same as above for client-side auth)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
