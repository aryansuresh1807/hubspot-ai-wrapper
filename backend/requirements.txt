# HubSpot AI Wrapper - Backend (FastAPI)
# Python 3.11+ recommended

# Web framework
fastapi==0.109.2
requests==2.31.0
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0
email-validator

# Supabase (pin httpx to avoid Client.__init__() "proxy" / "socket_options" errors)
supabase>=2.10.0,<3
httpx>=0.26.0,<0.28

# Environment
python-dotenv==1.0.1

# JWT & auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# LLM (OpenAI; for Anthropic use anthropic SDK instead)
openai==1.12.0

# Anthropic Claude (activity processing)
anthropic>=0.18.0

# Google OAuth & Gmail API
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.100.0
